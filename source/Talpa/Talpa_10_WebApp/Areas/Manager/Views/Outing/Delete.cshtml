@using Talpa_10_WebApp.Translations
@using Microsoft.AspNetCore.Mvc.Localization
@model Talpa_10_WebApp.ViewModels.OutingViewModel
@inject IHtmlLocalizer<Shared> SharedLocalizer

@{
    Layout = "_Layout";
    ViewData["Title"] = SharedLocalizer["Delete"];
    ViewData["DisableMainContainer"] = "True";
}


<div style="background-color: rgb(245, 245, 245); padding: 1em; font-size: 1.5em">
    <h1 style="text-align: center">@SharedLocalizer["Outing"]</h1>
    <div style="font-size: 0.8em; text-align: center">@SharedLocalizer["Delete"]</div>
</div>


<div class="container">
    @if (TempData.ContainsKey("Message"))
    {
        <div id="flashmessage" class="alert alert-@(TempData["MessageType"])">
            @TempData["Message"]
        </div>
    }
    <div class="col-md-8 mx-auto">
        <h3>@SharedLocalizer["Are you sure you want to delete this?"]</h3>
        <div>
            <hr/>
            <dl class="row">
                <dt class="col-sm-2">
                    @SharedLocalizer["Name"]
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </dl>

            <form asp-action="Destroy">
                <input type="hidden" asp-for="Id"/>
                <div class="form-group" style="display: flex; justify-content: space-between">
                    <a asp-action="Index" class="btn btn-secondary">@SharedLocalizer["BackToList"]</a>
                    <input type="submit" value="@SharedLocalizer["Delete"]" class="btn btn-danger" style="background-color: #D92A3B; border-color: #D92A3B;"/>
                </div>
            </form>
        </div>
    </div>
</div>